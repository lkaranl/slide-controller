<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seuapp">

    <!-- Permissões existentes -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Permissões para serviço em segundo plano -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
      ...>
      
      <!-- Declarar o serviço -->
      <service
          android:name=".VolumeButtonService"
          android:enabled="true"
          android:exported="false" />
          
      <!-- Adicione dentro da tag <application> -->
      <service
          android:name=".BackgroundControlService"
          android:enabled="true"
          android:exported="false" />
          
      <!-- Adicione isso para permitir iniciar o serviço por Intent -->
      <receiver
          android:name=".VolumeServiceReceiver"
          android:enabled="true"
          android:exported="true">
          <intent-filter>
              <action android:name="com.seuapp.START_VOLUME_SERVICE" />
              <action android:name="com.seuapp.STOP_VOLUME_SERVICE" />
          </intent-filter>
      </receiver>
          
      <!-- Adicione o serviço para funcionar com tela bloqueada -->
      <service
          android:name=".VolumeListenerService"
          android:enabled="true"
          android:exported="false" />
          
      <!-- Adicione esta linha dentro da tag <application> -->
      <meta-data android:name="com.google.android.gms.version" android:value="@integer/google_play_services_version" />
          
      <!-- Resto da configuração existente -->
      ...
    </application>
</manifest> 